[pytest]
testpaths =
    tests
asyncio_mode = auto
markers =
    integration: live API tests (skipped when the API is unreachable)
    api: tests that call a running Anchor API
filterwarnings =
    ignore::DeprecationWarning
